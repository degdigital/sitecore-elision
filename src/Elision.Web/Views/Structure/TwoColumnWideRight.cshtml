@model Elision.Web.Models.StructureViewModel
@using Elision.DynamicPlaceholders
<div class="columns columns--two columns--wide-right columns--@Model.RenderingUniqueId.ToShortID().ToString().ToLowerInvariant() @Model.CssClass">
    @Html.Partial("ColumnsColorSchemeStylesheet", Model)
    <div class="column">
        @Html.Sitecore().DynamicPlaceholder("rail")
    </div>
    <div class="column">
        @Html.Sitecore().DynamicPlaceholder("main")
    </div>
</div>
